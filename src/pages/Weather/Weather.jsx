import PlaceholderPage from '../../components/shared/PlaceholderPage'

/**
 * Weather — Phase 1 workspace placeholder.
 * Weather widgets currently live on the Dashboard. They will move into
 * this workspace (Overview / Forecast / Rain Totals / History / ET Trends)
 * in a later migration phase.
 */
export default function Weather() {
  return (
    <PlaceholderPage
      title="Weather"
      emptyTitle="Weather workspace coming together."
      emptyDesc="Weather widgets currently live on the Dashboard. They'll move here in a future phase."
    />
  )
}
