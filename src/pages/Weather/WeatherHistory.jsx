import PlaceholderPage from '../../components/shared/PlaceholderPage'

export default function WeatherHistory() {
  return (
    <PlaceholderPage
      title="Weather History"
      emptyTitle="No historical weather data."
      emptyDesc="Long-term weather records will appear here once collected."
    />
  )
}
