// Dashboard cross-module alerts — empty in production until live alerts arrive.
//
// Schema:
// DASHBOARD_ALERTS — [{ id, title, message, module, priority, status,
//                       timestamp, action, ... }]

export const DASHBOARD_ALERTS = []
